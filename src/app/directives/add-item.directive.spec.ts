import { DeleteItemDirective } from './add-item.directive';

describe('DeleteItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteItemDirective();
    expect(directive).toBeTruthy();
  });
});
